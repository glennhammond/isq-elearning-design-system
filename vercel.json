{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf /tmp/eds-build && cp -R .. /tmp/eds-build && python3 -m venv .venv && .venv/bin/pip install -r ../requirements.txt && .venv/bin/python /tmp/eds-build/generate.py && rm -rf generated && mkdir generated && cp -R /tmp/eds-build/site/. generated/",
  "outputDirectory": "generated"
}
